const mainName = "WFR Toolkit";
const signIn = `Sign In | ${mainName}`;
const signUp = `Sign Up | ${mainName}`;
const forgotPassword = `Forgot your Password | ${mainName}`;

const pagesTitleConstValues = {
  signIn,
  signUp,
  forgotPassword,
};

export default pagesTitleConstValues;
