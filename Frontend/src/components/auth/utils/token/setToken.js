async function setTokenToStorage(token: string) {
  // chrome.storage.local.set({ token: token }, function () {
  //   console.log("Token is set in local storage")
  // })
}

export default setTokenToStorage;
