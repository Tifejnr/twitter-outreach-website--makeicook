async function getTokenFromStorage() {
  // try {
  //   const storageResponse = await chrome.storage.local.get("token")
  //   const token = storageResponse.token
  //   if (token) {
  //     return token
  //   } else {
  //     return false
  //   }
  // } catch (error) {
  //   console.error("Error while fetching token from storage:", error)
  //   return false
  // }
}

export default getTokenFromStorage;
