import { allToolsVideosObj } from "../../videos/allToolsVideos";
const heading = `Follow up users who are yet to reply your Dms. Remember ?  " The fortune is in the follow-up. "`;
const overview = `

`;
const classicWayVideoUrl = allToolsVideosObj.automaticClientNameRetrievalDemo;

const followUpUsersObj = {
  heading,
  overview,
  classicWayVideoUrl,
};

export default followUpUsersObj;
