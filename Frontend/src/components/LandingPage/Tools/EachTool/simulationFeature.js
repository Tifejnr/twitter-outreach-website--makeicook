import { allToolsVideosObj } from "../../videos/allToolsVideos";
const heading = `"Payment Terms" and "Profile Options" Simulation`;
const overview =
  "Tired of the manual click dance for payment term options? WFR Toolkit introduces simulation magic! Sit back and watch as we handle the selection process for you. No more clicking – just a smooth, hands-free preparation for proposal submission.";

const classicWayVideoUrl = allToolsVideosObj.addingToBoardClassicWay;

export const simulationFeatureObj = {
  heading,
  overview,
  classicWayVideoUrl,
};
