const heading =
  "Save all your reply scripts and access them instantly while chatting with prospects with just a button click.";
const overview =
  "This tool allows easy removal of same Trello members from multiple Trello boards at once, eliminating the need to perform the task manually for each board.";

const accessToAllRepliesScriptsObj = {
  heading,
  overview,
};

export default accessToAllRepliesScriptsObj;
