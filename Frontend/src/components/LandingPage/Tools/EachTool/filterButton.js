import { allToolsVideosObj } from "../../videos/allToolsVideos";
const heading = "One-Click Filtering on Job Search Page";

const overview = `Simply save all your favorite filters once. 


  
Then, on the job search page, click a button to apply them all at once. 


  
No more tedious clicking through each filter individually – just instant access to tailored job listings. 


  
With WFR Toolkit, finding the right opportunity has never been easier.
`;

const classicWayVideoUrl = allToolsVideosObj.oneClickFilteringDemo;

export const filterButtonObj = {
  heading,
  overview,
  classicWayVideoUrl,
};
