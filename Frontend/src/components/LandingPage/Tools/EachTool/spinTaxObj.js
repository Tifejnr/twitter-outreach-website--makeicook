import { allToolsVideosObj } from "../../videos/allToolsVideos";
const heading =
  "Sent messages are spin-taxed with AI to bypass spam detection by Twitter";
const overview = `
Do you prioritise greeting client with their name in your cover letter ?

WFR Toolkit helps with automatic retrieval of client names, saving you valuable time and effort of searching through client histories to get the client name manually. 
`;
const classicWayVideoUrl = allToolsVideosObj.automaticClientNameRetrievalDemo;

const spinTaxObj = {
  heading,
  overview,
  classicWayVideoUrl,
};

export default spinTaxObj;
