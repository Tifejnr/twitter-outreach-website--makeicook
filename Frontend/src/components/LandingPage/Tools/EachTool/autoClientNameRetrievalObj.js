import { allToolsVideosObj } from "../../videos/allToolsVideos";
const heading = "Automatic Client Name Retrieval";
const overview =
  "Sick of sifting through client histories to get the client name to personalize your proposal? WFR Toolkit's got your back! Within just 30 seconds of being on the proposal submission page, your client's name magically pops up above your cover letter. No more hunting – just more time to craft a winning proposal!";

const classicWayVideoUrl = allToolsVideosObj.addingToBoardClassicWay;

const autoClientNameRetrievalObj = {
  heading,
  overview,
  classicWayVideoUrl,
};

export default autoClientNameRetrievalObj;
